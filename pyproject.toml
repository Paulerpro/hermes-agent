[project]
name = "hermes-agent"
version = "0.2.0"
description = "A financial research agent built on the Nous Research Hermes Agent framework, routed through OpenRouter."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "yfinance>=0.2.40",
]

[tool.uv]
package = false
